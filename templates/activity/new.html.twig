{% extends 'admin.html.twig' %}
{% block title %} {{ 'Domaines d\'activité' }} {% endblock %}
{% block body %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Creér un domaine d\'activité'|upper }}</h4>
                <a class="btn btn-square btn-outline-danger" href="{{ path('activity_index') }}">
                    <label><i class="ti-na"></i></label>
                </a>
            </header>
            <div class="card-body center-vh">
                {{ include('activity/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}