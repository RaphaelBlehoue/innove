{% extends 'admin.html.twig' %}
{% block title %} {{ 'section service'|upper }} {% endblock %}
{% block body %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'section service'|upper }}</h4>
                <a class="btn btn-square btn-outline-danger" href="{{ path('service_index') }}">
                    <label><i class="ti-na"></i></label>
                </a>
            </header>
            <div class="card-body center-vh">
                {{ include('service/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}