{% extends 'admin.html.twig' %}
{% block title %} {{ 'Sous catégories solutions' }} {% endblock %}
{% block body %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Liste'|upper }}</h4>
                <a class="btn btn-label btn-info" href="{{ path('category_new') }}">
                    <label><i class="ti-plus"></i></label>
                    {{ 'Ajouter un nouveau'}}
                </a>
            </header>
            <div class="card-body">
                <table class="table table-striped table-bordered" cellspacing="0" data-provide="datatables">
                    <thead>
                    <tr>
                        <th>{{ 'Nom ' }}</th>
                        <th>{{ 'Date de creation' }}</th>
                        <th>actions</th>
                    </tr>
                    </thead>
                    <tfoot>
                    <tr>
                        <th>{{ 'Nom ' }}</th>
                        <th>{{ 'Date de creation' }}</th>
                        <th>actions</th>
                    </tr>
                    </tfoot>
                    <tbody>
                    {% for category in categories %}
                        <tr>
                            <td>{{ category.name }}</td>
                            <td>{{ category.created ? category.created|date('Y-m-d H:i:s') : '' }}</td>
                            <td>
                                <a href="{{ path('category_show', {'id': category.id}) }}" class="btn btn-primary">{{ 'Afficher' }}</a>
                                <a href="{{ path('category_edit', {'id': category.id}) }}" class="btn btn-danger">{{ 'Modifier' }}</a>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="7">no records found</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}