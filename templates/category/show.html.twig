{% extends 'admin.html.twig' %}
{% block title %} {{ 'Sous catégories solutions' }} {% endblock %}
{% block body %}
    <div class="col-6 col-md-offset-6">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Sous catégories solutions détail'|upper }}</h4>
                {{ include('category/_delete_form.html.twig') }}
            </header>
            <!--<img class="card-img-top" src="{# asset(post.assertPath) #}" alt="Card image cap">-->
            <div class="card-body">
                <table class="table">
                    <tbody>
                    <tr>
                        <th>{{ 'Nom' }}</th>
                        <td>{{ category.name }}</td>
                    </tr>
                    <tr>
                        <th>Created</th>
                        <td>{{ category.created ? category.created|date('Y-m-d H:i:s') : '' }}</td>
                    </tr>
                    </tbody>
                </table>
            </div>
            <div class="card-footer">
                <a class="btn btn-label btn-dark" href="{{ path('category_index') }}">
                    <label><i class="ti-arrow-left"></i></label>
                    {{ 'Retour à la liste'}}
                </a>
                <a class="btn btn-label btn-danger" href="{{ path('category_edit', {'id': category.id}) }}">
                    <label><i class="ti-pencil-alt"></i></label>
                    {{ 'Modifier' }}
                </a>
            </div>
        </div>
    </div>
{% endblock %}