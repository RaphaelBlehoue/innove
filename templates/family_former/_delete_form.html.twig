<form method="post" action="{{ path('family_former_delete', {'id': family_former.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ family_former.id) }}">
    <button class="btn btn-float btn-danger"><i class="ti-trash"></i></button>
</form>