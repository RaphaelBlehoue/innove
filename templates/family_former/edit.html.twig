{% extends 'admin.html.twig' %}
{% block title %} {{ 'Famille de formation' }} {% endblock %}
{% block body %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Modifier famille de formation'|upper }}</h4>
                {{ include('family_former/_delete_form.html.twig') }}
            </header>
            <div class="card-body center-vh">
                {{ include('family_former/_form.html.twig', {
                    'button_label': 'Mise à jour', 'button_class': 'btn-danger'
                })
                }}
            </div>
        </div>
    </div>
{% endblock %}