{% extends 'admin.html.twig' %}
{% block title %} {{ 'Solutions' }} {% endblock %}
{% block body %}
    <div class="col-12">
        <div class="card">
            <header class="card-header">
                <h4 class="card-title">{{ 'Liste'|upper }}</h4>
                <a class="btn btn-label btn-info" href="{{ path('solution_new') }}">
                    <label><i class="ti-plus"></i></label>
                    {{ 'Ajouter un nouveau'}}
                </a>
            </header>
            <div class="card-body">
                <table class="table table-striped table-bordered" cellspacing="0" data-provide="datatables">
                    <thead>
                        <tr>
                            <th>{{ 'Nom' }}</th>
                            <th>{{ 'Sous-famille' }}</th>
                            <th>Created</th>
                            <th>actions</th>
                        </tr>
                    </thead>
                    <tfoot>
                        <tr>
                            <th>{{ 'Nom' }}</th>
                            <th>{{ 'Sous-famille' }}</th>
                            <th>Created</th>
                            <th>actions</th>
                        </tr>
                    </tfoot>
                    <tbody>
                    {% for solution in solutions %}
                        <tr>
                            <td>{{ solution.name }}</td>
                            <td>{{ solution.category.name }}</td>
                            <td>{{ solution.created ? solution.created|date('Y-m-d H:i:s') : '' }}</td>
                            <td>
                                <a href="{{ path('solution_show', {'id': solution.id}) }}" class="btn btn-primary">{{ 'Afficher' }}</a>
                                <a href="{{ path('solution_edit', {'id': solution.id}) }}" class="btn btn-danger">{{ 'Modifier' }}</a>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="9">no records found</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}